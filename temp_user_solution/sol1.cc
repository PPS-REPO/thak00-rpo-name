#include <cstdio>

int main() {
    int a, b, c;
    scanf("%d%d%d", &a, &b, &c);
    printf("%d\n", a + b + c);

    // FILE* fp = fopen("/Users/morgan/Documents/rn/PROJECT-PS/PPS-CODE-RUNNER/a.txt", "w");
    // fprintf(fp, "Hello World");
    // fclose(fp);
    return 0;
}